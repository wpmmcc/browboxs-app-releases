[workspace]
members = ["."]

[package]
name = "browboxs-desktop"
version = "0.1.0"
description = "browboxs fingerprint workbench desktop client"
authors = ["browboxs"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-opener = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
directories = "6"

[features]
default = []
wdio-e2e = ["dep:tauri-plugin-wdio-webdriver"]

[dependencies.tauri-plugin-wdio-webdriver]
version = "1"
optional = true

[target.'cfg(target_os = "linux")'.dependencies]
gtk = "0.18"
gdk-pixbuf = "0.18"
glib = "0.18"

# Match monorepo release hardening (desktop crate is its own workspace)
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
debug = false
incremental = false

